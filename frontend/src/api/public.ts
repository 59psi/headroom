/**
 * Unauthenticated public surface — what the login screen may fetch before
 * anyone has signed in.
 */

/** The site logo, served without a session so the login page can show it. */
export const PUBLIC_LOGO_URL = '/api/public/branding/logo';
