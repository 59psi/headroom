# The engines floor (Node >= 22.22, react-router 8's) is a hard error, not
# a warning: `npm ci` on too-old a Node fails here rather than building an
# image that misbehaves at runtime. setup.sh checks the same floor for the
# friendlier local message.
engine-strict=true
